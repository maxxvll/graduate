-- ============================================================================
-- V7: 云盘分享与群文件
-- 执行顺序: 7
-- 创建日期: 2026-03-22
-- 描述: 云盘分享表和群文件表已在 V1 中创建，此处添加 chat_message 的 is_recalled 字段
-- 备注: 相关表已在 V1__Init_Schema.sql 中创建
-- ============================================================================

-- ================================================
-- chat_message 表字段扩展
-- ================================================

-- 检查字段是否存在，不存在则添加
-- 注意：V1 中的 chat_message 表已包含基础字段
-- 此处添加可选的撤回标记字段（如果尚未存在）

-- ALTER TABLE chat_message
-- ADD COLUMN IF NOT EXISTS is_recalled TINYINT(1) DEFAULT 0 COMMENT '是否已撤回：0-否，1-是';

-- 此版本保留用于未来扩展
